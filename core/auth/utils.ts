// import { getItem, removeItem, setItem } from '../storage';
// import * as SecureStore from 'expo-secure-store';

// const TOKEN = 'token';

// export type TokenType = {
//   access: string;
//   refresh: string;
// };

// export const getToken = () => getItem<TokenType>(TOKEN);
// export const getToken = async () => await SecureStore.getItemAsync('accessToken');
// export const removeToken = () => removeItem(TOKEN);
// export const removeToken = async () => await SecureStore.deleteItemAsync('accessToken');
// export const setToken = (value: TokenType) => setItem<TokenType>(TOKEN, value);
// export const setToken = async (value: string) => await SecureStore.setItemAsync(TOKEN, value);
